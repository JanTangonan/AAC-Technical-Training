PLCDBPanel.js
    appendMemoCounter()
        $memofield.parent().append(divMemoCounter); > $memofield.after(divMemoCounter);

- Please switch the display style of memo counter so that the required message will still show to the right if there is available space